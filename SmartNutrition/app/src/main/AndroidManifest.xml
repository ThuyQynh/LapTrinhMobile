<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══ QUYỀN CHUNG ═══ -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ═══ Module 2: Camera + Barcode ═══ -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- ═══ Module 4: Alarm + Notification ═══ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ═══ Module 5: Xuất PDF ═══ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".SmartNutritionApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartNutrition">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartNutrition">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ═══ Module 4: Receivers cho nhắc nhở ═══ -->
        <receiver android:name=".habit.receiver.WaterReminderReceiver" android:exported="false" />
        <receiver android:name=".habit.receiver.WaterActionReceiver" android:exported="false" />
        <receiver android:name=".habit.receiver.VitaminReminderReceiver" android:exported="false" />
        <receiver android:name=".habit.receiver.CustomReminderReceiver" android:exported="false" />
        <receiver android:name=".habit.receiver.SleepReminderReceiver" android:exported="false" />
        <receiver android:name=".habit.receiver.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ═══ Module 5: FileProvider cho xuất PDF ═══ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
